"""
Legacy setup.py kept for compatibility with tooling that still invokes it.
All metadata and configuration are defined in pyproject.toml (PEP 621).
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
