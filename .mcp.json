{
  "mcpServers": {
    "proxmox-guild-a": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "proxmox-mcp-guild-a",
        "/bin/bash",
        "-c",
        "cd /app && source .venv/bin/activate && python -m proxmox_mcp.server"
      ]
    }
  }
}
