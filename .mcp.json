{
  "mcpServers": {
    "proxmox-guild-a": {
      "command": "ssh",
      "args": [
        "-i",
        "~/.ssh/proxmox_guild_a",
        "guildvm@100.64.198.100",
        "docker",
        "exec",
        "-i",
        "proxmox-mcp-guild-a",
        "/bin/bash",
        "-c",
        "cd /app && source .venv/bin/activate && python -m proxmox_mcp.server"
      ]
    }
  }
}
